# Moved → brain/

> **This directory is no longer the memory system.**
>
> The institutional memory of The People's Home has been migrated and expanded
> into the **`brain/`** directory at the root of this repository.
>
> → **Go to [`../../brain/README.md`](../../brain/README.md)**

---

The content that was here has been:

- Migrated in full to `brain/` (no content was lost)
- Expanded with new files: `AI_ONBOARDING.md`, `SESSION_HANDOFF.md`,
  `PROJECT_GLOSSARY.md`, `patterns/`, `research/`, `assets/`
- Restructured: `canon/` split into `brain/vision/` and `brain/architecture/`
- `DECISIONS.md` and `HISTORY.md` moved to `brain/decisions/` and `brain/history/`

The migration was done on **2026-06-27** via `git mv docs/memory brain`.
Git history for all files is preserved — use `git log --follow brain/<file>` to see it.
